rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGOLE TEMPORANEE - PIÙ PERMISSIVE
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}